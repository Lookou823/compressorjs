# GitHub 提交脚本

此脚本用于将当前所有修改提交到 GitHub 远程仓库。

## 执行步骤

### 1. 检查当前 Git 状态
```bash
git status
```

### 2. 添加所有修改的文件
```bash
# 添加所有修改和新增的文件
git add .

# 或者分别添加特定文件
git add ".trae/documents/修复3MB PNG导致无限压缩问题与重复 blob 请求.md"
git add .cursor/
git add scripts/publish.sh
git add src/utils.imageCompressor.js
git add test-images.html
git add test-npm-publish-command.md
```

### 3. 创建符合规范的提交信息
根据 Conventional Commits 规范，提交信息格式为：
```
<type>(<scope>): <subject>

<body>

<footer>
```

执行提交：
```bash
git commit -m "feat: 添加图片压缩工具函数和发布脚本

- 新增 utils.imageCompressor.js 工具函数，支持强制使用 Web Worker 进行图片压缩
- 添加 publish.sh 发布脚本，简化 npm 发布流程
- 添加测试文件 test-images.html 用于本地测试
- 更新文档说明修复 3MB PNG 无限压缩问题
- 添加 .cursor 配置目录用于开发工具配置"
```

### 4. 推送到远程仓库
```bash
# 推送到 main 分支
git push origin main

# 如果推送失败，可能需要先拉取远程更改
git pull origin main --rebase
git push origin main
```

### 5. 验证提交是否成功
```bash
# 检查远程仓库状态
git status

# 查看最近的提交记录
git log --oneline -5

# 验证远程分支是否已更新
git log origin/main --oneline -5
```

## 一键执行脚本

如果需要一键执行所有步骤，可以使用以下脚本：

```bash
#!/bin/bash

# 设置错误时退出
set -e

echo "📋 检查 Git 状态..."
git status

echo ""
echo "📦 添加所有修改的文件..."
git add .

echo ""
echo "💾 创建提交..."
git commit -m "feat: 添加图片压缩工具函数和发布脚本

- 新增 utils.imageCompressor.js 工具函数，支持强制使用 Web Worker 进行图片压缩
- 添加 publish.sh 发布脚本，简化 npm 发布流程
- 添加测试文件 test-images.html 用于本地测试
- 更新文档说明修复 3MB PNG 无限压缩问题
- 添加 .cursor 配置目录用于开发工具配置"

echo ""
echo "🚀 推送到远程仓库..."
git push origin main

echo ""
echo "✅ 验证提交状态..."
git status

echo ""
echo "✨ 提交成功！"
```

## 注意事项

1. **提交前检查**：确保所有修改都是预期的，可以使用 `git diff` 查看具体变更
2. **提交信息规范**：遵循 Conventional Commits 规范，类型包括：
   - `feat`: 新功能
   - `fix`: 修复 bug
   - `docs`: 文档更新
   - `style`: 代码格式调整
   - `refactor`: 代码重构
   - `test`: 测试相关
   - `chore`: 构建/工具相关
3. **冲突处理**：如果推送时出现冲突，需要先解决冲突再推送
4. **权限检查**：确保有推送到远程仓库的权限

## 故障排查

### 如果推送失败：
```bash
# 1. 检查远程仓库连接
git remote -v

# 2. 拉取最新更改
git pull origin main --rebase

# 3. 解决冲突后再次推送
git push origin main
```

### 如果提交信息不符合规范：
```bash
# 修改最后一次提交信息
git commit --amend -m "新的提交信息"

# 强制推送（谨慎使用）
git push origin main --force
```

